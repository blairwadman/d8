(function ($, Drupal) {
    'use strict';

    Drupal.behaviors.sweet = {
        attach: function attach(context, settings) {
            //swal("Hello world!");
        }
    };
})(jQuery, Drupal);
