<div id="page">
    <section class="content">
        {{ page.content }}
    </section>
    {% if page.left %}
        <aside class="left">
            {% if sidebar_mpu %}
                {{ sidebar_mpu }}
            {%  endif %}
            {{ page.left }}
        </aside>
    {% endif %}
</div>
